# Starter pack of Go Fiber in Docker

This repo is for starter pack for Go Fiber the most powerful web framework.

I have created `Makefile` for easy to run this docker by just:

```shell
make build
make run
```
